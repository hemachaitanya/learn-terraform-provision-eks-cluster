pool: 'Default'
trigger:
- main

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'Docker'
    repository: 'spc'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'
